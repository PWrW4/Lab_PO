#include "Sword.h"
#include "Header.h"

int Sword::getDamage(int playerStrength, Monster& monster)
{
	if (true/*monster.getType()*/)
	{
	MonsterType:Dragon;
		playerStrength += 10;
	}
	return playerStrength;
}
